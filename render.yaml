services:
  - type: web
    name: tournament-management-app
    env: docker
    plan: free
    branch: qa
    dockerfilePath: ./Dockerfile
    dockerCommand: ./entrypoint.sh
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: MSSQL_SA_PASSWORD
        sync: false
      - key: PORT
        value: 80
      - key: ConnectionStrings__DefaultConnection
        value: "Server=${RENDER_PG_HOST};Database=Torneo;User Id=sa;Password=${MSSQL_SA_PASSWORD};TrustServerCertificate=true"
    healthCheckPath: /health